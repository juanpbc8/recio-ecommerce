{
  "name": "recio-ecommerce",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo para un E-commerce",
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "npm@11.12.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
