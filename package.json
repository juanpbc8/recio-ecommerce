{
  "name": "recio-ecommerce",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo para un E-commerce",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "npm@11.12.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:generate": "turbo run generate --filter=@repo/database",
    "db:push": "turbo run push --filter=@repo/database"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
